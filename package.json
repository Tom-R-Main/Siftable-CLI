{
  "name": "@siftable/cli",
  "version": "0.5.7",
  "description": "Siftable CLI — tasks, knowledge, agents, work queues, and automation harness tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sift": "bin/run.js",
    "siftable": "bin/run.js",
    "exf": "bin/run.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:vitest": "vitest run --config vitest.config.ts",
    "prepublishOnly": "npm run build",
    "prepack": "npm run build && oclif manifest",
    "postpack": "rm -f oclif.manifest.json"
  },
  "oclif": {
    "bin": "sift",
    "dirname": "siftable",
    "commands": "./dist/commands",
    "topicSeparator": " ",
    "plugins": [
      "@oclif/plugin-help"
    ],
    "topics": {
      "auth": {
        "description": "Authentication commands"
      },
      "tasks": {
        "description": "Task management"
      },
      "projects": {
        "description": "Project management"
      },
      "notes": {
        "description": "Knowledge notes"
      },
      "calendar": {
        "description": "Calendar events"
      },
      "people": {
        "description": "People and contacts"
      },
      "organizations": {
        "description": "Organizations and companies"
      },
      "codebase": {
        "description": "Code indexing and search"
      },
      "code": {
        "description": "Code tools"
      },
      "code:memory": {
        "description": "Code memory and facts"
      },
      "vault": {
        "description": "Secrets vault"
      },
      "datasets": {
        "description": "Structured datasets"
      },
      "documents": {
        "description": "Document upload"
      },
      "agents": {
        "description": "Agent aliases"
      },
      "work": {
        "description": "Agent work queue"
      },
      "codex": {
        "description": "Codex automation helpers"
      }
    }
  },
  "dependencies": {
    "@oclif/core": "^4",
    "@oclif/plugin-help": "^6",
    "@siftable/mcp-server": "^1.2.6"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "nock": "^13.5.0",
    "oclif": "^4",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "cli",
    "siftable",
    "ai",
    "tasks",
    "calendar",
    "knowledge-base",
    "agents"
  ],
  "author": "Siftable",
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "oclif.manifest.json"
  ],
  "publishConfig": {
    "access": "public"
  }
}
